# فاز ۴ — مرحلهٔ A: ماشین‌حساب‌ها، شمارنده و علاقه‌مندی‌ها

**وضعیت:** ✅ تحویل‌شده و روی Production فعال — `2026-08-13`
**Commit:** `50356c4` · **Version ID:** `677526fa-0181-43cd-93b6-cf4db836bc19`

---

## ۱. چه چیزی اضافه شد

دستهٔ جدید **🧰 Everyday Tools** با هفت زیرگروه ساخته شد و اولین زیرگروه آن (**📐 Calculators**) به‌همراه سیستم **⭐ Favorites** پیاده‌سازی شد. تعداد ابزارها از **۶۵ → ۷۷** رسید (`/health` تأیید می‌کند).

### ابزارهای جدید (۱۲ مورد)

| # آیتم | ابزار | شناسه | توضیح کوتاه |
|---|---|---|---|
| ۲ | محاسبه‌گر درصد | `percent_calc` | زبان طبیعی: `15% of 200`، `120 to 150`، `200 + 15%`، `30 is 15% of what`، `diff 40 60` |
| ۴ | BMI | `bmi_calc` | متریک و امپریال + دسته‌بندی WHO |
| ۵ | ساختمانی | `construction_calc` | مساحت/حجم/بتن/دیوار/آجر/کاشی/رنگ + درصد پرت قابل تنظیم + نمایش فرضیات |
| ۶ | هزینهٔ سوخت | `fuel_calc` | مسافت/مصرف/قیمت، دوطرفه |
| ۷ | برق | `electricity_calc` | توان/ولتاژ/جریان/انرژی/هزینه + نمایش فرمول |
| ۸ | مساحت و حجم | `geometry_calc` | مستطیل، مربع، دایره، مثلث، استوانه، مکعب، کره، مخروط |
| ۹ | تبدیل ارز | `currency_convert` | نرخ واقعی از دو Provider، بدون هیچ نرخ ساختگی |
| ۱۰ | انعام | `tip_calc` | تقسیم بین چند نفر + گرد کردن |
| ۱۱ | اقساط | `installment_calc` | قسط ماهانه، کل بازپرداخت، کل سود |
| ۱۲ | سود مرکب | `compound_calc` | دوره‌های مرکب‌شدن مختلف + واریز دوره‌ای |
| ۱۳ | سود و زیان | `profit_calc` | کارمزد ثابت یا درصدی + درصد بازده |
| ۱۴ | مالیات | `tax_calc` | نرخ کاملاً از ورودی کاربر (بدون Hardcode)، هم افزودن و هم جدا کردن مالیات |
| ۵۰ | علاقه‌مندی‌ها | — | ⭐ روی هر صفحهٔ ابزار + «⭐ My Favorites» در Home |

### ابزارهایی که Extend شدند (طبق قانون «نسخهٔ دوم نساز»)

| ابزار موجود | چه چیزی اضافه شد |
|---|---|
| `calculator` (آیتم ۳) | توان، ریشه، پرانتز، توابع دوآرگومانی `root/logb/min/max`، فاکتوریل، نمادهای `√ ÷ ×`، ارقام فارسی — **بدون `eval`**، پارسر دستی امن |
| `unit_convert` (آیتم ۱) | افزودن مساحت، حجم، فشار، انرژی، توان به دسته‌های موجود |
| `text_counter` (آیتم ۱۵) | کاراکتر، بدون فاصله، کلمه، خط، جمله و پاراگراف — با تشخیص جملهٔ فارسی (`؟ ! .`) و انگلیسی |

### زیرساخت

- `src/tools/everyday/fields.ts` — پارسر مشترک ورودی `key: value` با نام‌های مستعار فارسی/انگلیسی و پشتیبانی از ارقام فارسی.
- `src/db/favorites.ts` + جدول `favorites` در D1 (کلید مرکب، سقف ۳۰ مورد در هر کاربر، حذف آبشاری، ایندکس روی `(user_id, added_at DESC)`).
- ناوبری: دستورهای `/everyday`، `/favorites`، `/fav` و کال‌بک‌های `grp:<group>:<page>`، `fav:<page>`، `favt:<toolId>` — همگی زیر سقف ۶۴ بایتی تلگرام.

---

## ۲. تصمیم‌های مهندسی قابل توجه

**تبدیل ارز هرگز نرخ نمی‌سازد.** Provider اول Frankfurter (نرخ‌های ECB) و در صورت شکست، ExchangeRate-API. اگر هر دو از دسترس خارج باشند، ابزار **خطای صریح** می‌دهد و هیچ عددی نشان نمی‌دهد — نه نرخ کش‌شدهٔ قدیمی، نه تقریب. منبع و تاریخ آخرین به‌روزرسانی همیشه کنار نتیجه چاپ می‌شود. نرخ‌های موفق ۱۰ دقیقه کش می‌شوند؛ **شکست‌ها هرگز کش نمی‌شوند**.

**تومان.** Frankfurter ریال ندارد؛ برای `IRR`/`IRT` مستقیماً به Provider دوم می‌رود و صراحتاً اعلام می‌کند که نرخ رسمی است و «۱ تومان = ۱۰ ریال» اعمال شده — چون نرخ بازار آزاد در هیچ API عمومی معتبری نیست و نمایش نرخ رسمی به‌جای آن، بدون برچسب، گمراه‌کننده می‌بود.

**Favorites عمداً از `tool_usage` جداست.** آن جدول «پرکاربردترین‌ها» را از رفتار استخراج می‌کند؛ این یکی یک انتخاب صریح است. ادغامشان یعنی نمی‌شد ابزاری کم‌استفاده اما مهم را ستاره‌دار نگه داشت.

---

## ۳. دو باگی که تست‌ها لو دادند (و رفع شدند)

1. **`profit_calc` با بهای خرید صفر** بازده «۰٪» چاپ می‌کرد در حالی که سود واقعی وجود داشت — درصد بازده روی مبنای صفر تعریف‌نشده است. حالا ورودی رد می‌شود با پیام روشن.
2. **`toggleFavorite` موفقیت دروغین گزارش می‌کرد.** تابع `isFavorite` خطای دیتابیس را می‌بلعید و `false` برمی‌گرداند؛ در نتیجه اگر خود کوئری بررسی وجود شکست می‌خورد، مسیر INSERT اجرا می‌شد و به کاربر «اضافه شد» نشان داده می‌شد بدون آنکه چیزی ذخیره شده باشد. حالا مسیر toggle از یک نسخهٔ سخت‌گیر استفاده می‌کند و خطا را به‌صورت هشدار به کاربر برمی‌گرداند؛ نسخهٔ بخشنده فقط برای رندر دکمه استفاده می‌شود.

---

## ۴. تست‌ها

**۸۴۰ تست در ۳۱ فایل — همه سبز** (پیش از این مرحله: ۷۵۱ در ۲۷).

| فایل جدید | تعداد | پوشش |
|---|---|---|
| `tests/unit/everyday-calculators.test.ts` | ۴۸ | هر ۱۲ ماشین‌حساب + پارسر فیلد + شمارندهٔ متن، با حالت‌های valid / invalid / empty / edge / large |
| `tests/unit/everyday-currency.test.ts` | ۱۵ | چهار حالت لازم API: موفق · Timeout · Error · پاسخ نامعتبر — به‌علاوهٔ fallback، پاسخ غیر‌JSON، نرخ صفر/منفی، رفتار کش |
| `tests/unit/favorites.test.ts` | ۱۰ | افزودن/حذف، ترتیب، جداسازی کاربران، سقف، هرس ابزارهای حذف‌شده، خطای دیتابیس |
| `tests/integration/everyday-nav.test.ts` | ۱۶ | مسیر واقعی روتر: دستورها، همهٔ زیرگروه‌ها، گروه ناشناخته، شمارهٔ صفحهٔ بی‌معنا، سقف ۶۴ بایت، دکمهٔ بازگشت، کال‌بک‌های ⭐ |

یک اصلاح در `tests/unit/registry.test.ts` لازم شد: قانون یکپارچگی، ابزارهای `network: true` را فقط در دسته‌های `network` و `utilities` مجاز می‌دانست و `currency_convert` را رد می‌کرد؛ دستهٔ `everyday` به فهرست مجاز اضافه شد.

**خط لولهٔ کامل:** Type Check (هر دو tsconfig) ✅ · ESLint `--max-warnings=0` ✅ · ۸۴۰ تست ✅ · Build (dry-run) ✅ · اسکن نشت اطلاعات ✅ · GitHub Actions روی `50356c4` ✅ success.

---

## ۵. وضعیت انتشار

| مورد | وضعیت |
|---|---|
| GitHub | `48f3191 → 50356c4` روی `main` — push شد، CI سبز |
| D1 (remote) | schema اعمال شد؛ جدول `favorites` ساخته شد (۶ جدول) |
| Cloudflare Worker | deploy شد — `https://devnet-toolbox.nikiaaaaacr.workers.dev` |
| `/health` | `ok: true` · **tools: 77** · kv/d1/token همه `true` |
| منوی دستورهای تلگرام | `setMyCommands` اجرا شد (شامل `/everyday` و `/favorites`) |
| Webhook | تنظیم شد با secret تازه‌چرخانده‌شده؛ درخواست با secret اشتباه → **401** |

### Smoke Test روی Production

همهٔ موارد زیر مستقیماً روی webhook زنده اجرا شدند و `200` گرفتند: `/start` · `/everyday` · `/favorites` · `grp:calculators:1` · `tool:percent_calc` · `favt:percent_calc` · `fav` · اجرای `percent_calc` با ورودی `15% of 200` · اجرای `currency_convert` با ورودی `100 USD to EUR`.

تأیید نهایی از خود دیتابیس Production:

```
FAVORITES:    [{'tool_id': 'percent_calc'}]
RECENT USAGE: currency_convert(1), percent_calc(1), …
```

یعنی هم ستاره‌کردن واقعاً در D1 نوشته شد و هم تبدیل ارز واقعاً تا انتها با نرخ زنده اجرا شد.

> **نکتهٔ امنیتی:** مقدار `WEBHOOK_SECRET` قبلی در دسترس نبود، بنابراین با یک مقدار تصادفی تازه جایگزین و هم‌زمان روی Worker و Telegram ست شد. هیچ Token/Secret‌ای در سورس، commit یا این گزارش وجود ندارد.

---

## ۶. مرحلهٔ بعد

**مرحلهٔ B — 🧠 Productivity:** یادداشت‌های شخصی (۳۷)، یادآور با Cloudflare Scheduler (۳۶)، پومودورو (۳۸)، تقویم (۳۹)، تولید فهرست کارها (۴۸)، برنامه‌ریز مطالعه (۴۹)، قالب‌ساز پرامپت (۴۷).

سپس C (اسناد/آرشیو) · D (اطلاعات) · E (مدیا/AI).

**محدودیت‌های شناخته‌شده که در گزارش پایانی فاز ۴ مستند خواهند شد:** Cloudflare Workers AI مدل OCR و Background Remover ندارد و Workers فاقد Canvas API است؛ این دو آیتم (۲۱ و ۲۸) به‌جای Placeholder ناقص، به‌عنوان پیاده‌نشده گزارش می‌شوند — طبق دستور صریح شما.
