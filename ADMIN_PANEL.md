# پنل ادمین وب — گزارش تحویل

پنل مدیریت تحت وب برای ربات **DevNet Toolbox**، به صورت یک Worker **مستقل و جداگانه** ساخته و روی Cloudflare منتشر شد.

- **آدرس پنل:** https://devnet-admin.nikiaaaaacr.workers.dev
- **Worker ربات (بدون تغییر در آدرس):** https://devnet-toolbox.nikiaaaaacr.workers.dev
- **مخزن:** https://github.com/amirnikzadcr-code/devnet-toolbox — شاخهٔ `main`، آخرین commit `32ad318`
- **وضعیت CI:** ✅ success

---

## ۱. معماری

پنل یک Worker دوم است (`devnet-admin`) که به **همان** پایگاه داده و فضای کلید-مقدار ربات متصل است:

| منبع | مقدار | اشتراک با ربات |
|---|---|---|
| D1 | `devnet_toolbox` | ✅ همان دیتابیس |
| KV | `STATE` | ✅ همان namespace |
| کد ربات | `src/` | بدون تغییر ساختاری |
| کد پنل | `admin/src/` | جدا و مستقل |

دلیل جدا بودن Worker: سطح حملهٔ پنل (کوکی، نشست، فرم) هرگز با مسیر `webhook` ربات مخلوط نمی‌شود و هر کدام مستقل deploy می‌شوند. اشتراک D1/KV باعث می‌شود پنل داده‌های **واقعی و زنده** را ببیند، نه یک کپی.

**فایل‌های افزوده‌شده:** `admin/src/{index,auth,data,telegram,views,types}.ts` + `admin/wrangler.jsonc` + `admin/tsconfig.json`.
**تغییر در کد ربات:** فقط دو مورد کوچک — تابع `isBanned` در `src/services/state.ts` و یک بررسی در `src/bot/router.ts`. هیچ قابلیت قبلی حذف یا بازنویسی نشد.

---

## ۲. احراز هویت (دو عاملی)

ورود دو مرحله‌ای است:

1. **رمز عبور** — Secret به نام `ADMIN_PASSWORD` روی Worker پنل. مقایسه با الگوریتم زمان-ثابت (`safeEqual`) تا نشت زمانی ندهد.
2. **کد ۶ رقمی** — به تلگرام شما (`ADMIN_CHAT_ID`) ارسال می‌شود. اعتبار ۵ دقیقه، **یک‌بارمصرف** (چه درست چه غلط سوزانده می‌شود تا حدس زدن پیاپی ممکن نباشد).

نکات امنیتی پیاده‌شده:

- **نشست در KV** با توکن امضاشدهٔ HMAC-SHA256 (`SESSION_SECRET`). دستکاری توکن یا امضای اشتباه ⇒ رد. خروج، نشست را سمت سرور باطل می‌کند (تست شد: کوکی بعد از logout دیگر کار نمی‌کند).
- کوکی با `HttpOnly` + `Secure` + `SameSite=Strict` + `Max-Age=28800` (۸ ساعت).
- **محدودسازی تلاش**: ۵ تلاش ناموفق برای هر عامل، سپس ۱۵ دقیقه قفل — به تفکیک IP.
- پیام خطا **عمداً مبهم** است («اطلاعات ورود نادرست است») تا مشخص نشود کدام عامل غلط بوده.
- تمام درخواست‌های POST بررسی مبدأ (CSRF) می‌شوند؛ مبدأ بیگانه ⇒ 403.

---

## ۳. امکانات پنل

| صفحه | امکانات |
|---|---|
| **داشبورد** | کاربران، کاربران جدید امروز، فعال امروز/هفته، تعداد درخواست‌ها و اجرای ابزارها، نمودار ۱۴ روزه، پرکاربردترین ابزارها، وضعیت Webhook، آخرین رویدادها |
| **کاربران** | جستجو، مرتب‌سازی (آخرین بازدید/تعداد اجرا/تاریخ عضویت)، صفحه‌بندی، فیلتر مسدودها |
| **جزئیات کاربر** | پروفایل، ابزارهای پرمصرف او، علاقه‌مندی‌ها، مسدودسازی/رفع مسدودی، ارسال پیام مستقیم، حذف کامل داده‌ها |
| **ابزارها** | آمار تک‌تک ۷۷ ابزار: تعداد اجرا، کاربران یکتا، آخرین استفاده، سهم درصدی |
| **پیام همگانی** | ارسال به همه / فعال ۷ روز / فعال ۳۰ روز / فارسی‌زبان‌ها / انگلیسی‌زبان‌ها + تاریخچه |
| **تنظیمات ربات** | مشاهده و تنظیم Webhook، همگام‌سازی دستورات، ویرایش توضیحات ربات (فارسی و انگلیسی) |
| **رویدادها** | تاریخچهٔ کامل و فقط-افزودنی هر عمل مدیریتی |
| **📡 مانیتور زنده** | جریان لحظه‌ای فعالیت کاربران، نبض ۵ دقیقه/۱ ساعت، فیلتر بر اساس نوع رویداد، مصرف سهمیهٔ Cloudflare — با تازه‌سازی خودکار |
| **جزئیات پیام همگانی** | وضعیت تحویل تک‌تک گیرندگان (`/broadcast/:id`) + تعامل پس از ارسال |

**مسدودسازی واقعاً کار می‌کند:** پنل کلید `ban:<userId>` را در همان KV می‌نویسد و ربات در `handleUpdate` بلافاصله پس از فیلتر تکراری‌ها آن را می‌خواند. کاربر مسدود **بی‌صدا** رد می‌شود (هم پیام، هم دکمه) — پاسخ ندادن عمدی است تا اسپمر سیگنالی برای واکنش نگیرد.
`isBanned` در صورت خطای KV **fail-open** است: قطعی KV نباید کل کاربران را از ربات محروم کند.

### مانیتور زنده — و چرا «سین» وجود ندارد

مانیتور به چهار سؤال پاسخ می‌دهد: چه کسی همین الان از ربات استفاده می‌کند، چه کسی پیام همگانی را واقعاً دریافت کرد، چقدر از سهمیهٔ رایگان Cloudflare مانده، و همهٔ این‌ها بدون رفرش دستی.

**۱) فقط متادیتا — متن هرگز ذخیره نمی‌شود.** جدول `activity` فقط شناسهٔ کاربر، نوع رویداد، یک برچسب کوتاه (مثل `base64_encode` یا `/start`)، موفق/ناموفق و مدت اجرا را نگه می‌دارد. تابع `sanitiseDetail` هر چیزی خارج از مجموعه‌کاراکترهای مجاز شناسه را حذف و طول را به ۴۸ کاراکتر محدود می‌کند، پس حتی اگر روزی جایی اشتباه سیم‌کشی شود، متن قابل‌خواندنی وارد دیتابیس نمی‌شود. رکوردها پس از **۷ روز** خودکار پاک می‌شوند.

> این ادعا در production آزمایش شد: ابزار `base64_encode` روی ورودی واقعی `DevNet secret payload` اجرا شد و پرس‌وجو روی جدول `activity` صفر رکورد حاوی آن متن برگرداند؛ تنها چیز ذخیره‌شده `base64_encode` بود.

**۲) «سین» در Bot API وجود ندارد.** تلگرام هیچ متد یا آپدیتی برای read receipt به ربات‌ها نمی‌دهد؛ تیک دوم فقط برای حساب‌های کاربری (MTProto) است و رسیدن به آن یعنی خارج شدن از معماری Webhook این پروژه. به‌جای ساختن یک عدد بی‌معنی، جدول `broadcast_delivery` نتیجهٔ **واقعی** `sendMessage` را برای هر گیرنده ثبت می‌کند: `sent` یا `failed` به‌همراه دلیل تلگرام (مثلاً «bot was blocked by the user»). صفحهٔ `/broadcast/:id` در کنار آن «تعامل پس از ارسال» را نشان می‌دهد — کاربرانی که بعد از دریافت پیام با ربات کار کرده‌اند. این نشانهٔ قوی توجه است، اما صریحاً «سین» نامیده نمی‌شود.

**۳) سهمیهٔ Cloudflare.** از GraphQL Analytics API با datasetهای `workersInvocationsAdaptive` و `d1AnalyticsAdaptiveGroups` خوانده می‌شود. اگر `CF_ANALYTICS_TOKEN` تنظیم نشده باشد یا توکن permission لازم را نداشته باشد، کارت **عدد صفر جعلی نمی‌سازد** و به‌جایش توضیح می‌دهد چه توکنی با چه دسترسی‌ای لازم است.

**۴) لایو بودن بدون JavaScript.** CSP پنل عمداً `script-src` ندارد، بنابراین تازه‌سازی با `<meta http-equiv="refresh">` انجام می‌شود (پیش‌فرض ۵ ثانیه، قابل تنظیم با `?refresh=` و محدودشده به بازهٔ ۳ تا ۶۰ ثانیه). هیچ استثنایی در CSP باز نشد.

**تله‌متری هرگز درخواست کاربر را نمی‌شکند:** `recordActivity` خطای D1 را می‌بلعد و فقط لاگ می‌کند، و ثبت تحویل هر batch در try/catch جداست.

**رابط کاربری:** فارسی، راست‌به‌چپ، تم تیره. بدون فریم‌ورک، بدون CDN، بدون فونت خارجی — HTML سمت سرور با CSS درون‌خطی زیر یک CSP سخت‌گیرانه (`default-src 'none'`). تمام مقادیر ورودی کاربر escape می‌شوند.

---

## ۴. تست‌ها

مجموع سوئیت: **۱۰۳۲ تست در ۳۵ فایل — همگی سبز.** از این تعداد **۱۳۶ تست جدید** برای این کار نوشته شد:

| فایل | تعداد | پوشش |
|---|---|---|
| `tests/unit/admin-auth.test.ts` | ۳۵ | مقایسهٔ زمان-ثابت، تولید کد، یک‌بارمصرف بودن چالش، دستکاری/انقضا/ابطال نشست، پرچم‌های کوکی، محدودسازی تلاش، بررسی مبدأ |
| `tests/unit/admin-data.test.ts` | ۴۳ | صحت کوئری‌ها، تلاش تزریق SQL در جستجو و مرتب‌سازی، محدودسازی صفحه‌بندی، مسدودسازی/حذف و انعکاس در KV، بریدن طول ورودی‌ها |
| `tests/unit/admin-views.test.ts` | ۵۴ | escape شدن هر مقدار کاربرساخته (XSS)، حالت خالی هر صفحه، RTL، نبود منبع خارجی، عدم نمایش Secret |
| `tests/integration/admin-panel.test.ts` | ۵۳ | چرخهٔ کامل ورود، دروازهٔ احراز هویت، CSRF، تمام صفحات، اقدامات روی کاربر، پیام همگانی، تنظیمات ربات |
| `tests/integration/router.test.ts` (افزوده) | ۷ | رد شدن کاربر مسدود در پیام و دکمه، عدم تأثیر بر بقیه، رفع مسدودی، عدم ثبت آمار، fail-open هنگام قطعی KV |

هر صفحه از نظر **نشت اطلاعات** هم تست می‌شود: هیچ‌کدام از `BOT_TOKEN`، رمز عبور یا `SESSION_SECRET` نباید در خروجی HTML ظاهر شود، و خطای ۵۰۰ نباید stack trace یا متن خطای پایگاه داده را لو بدهد.

---

## ۵. مشکلاتی که پیدا و رفع شد

**۱) خطای ۵۰۰ روی «رفع مسدودی» و «حذف کاربر» (باگ واقعی production).**
این دو عمل هیچ فیلد فرمی ندارند. وقتی درخواست بدون بدنه و بدون `Content-Type` برسد، `request.formData()` استثنا پرتاب می‌کند و پاسخ ۵۰۰ می‌شود. **تست‌ها این را نگرفته بودند** چون کمکی تست همیشه بدنهٔ urlencoded می‌فرستاد؛ فقط در smoke test زندهٔ بعد از انتشار پیدا شد. رفع: تابع `readForm` که بدنهٔ غیرقابل‌تجزیه را «بدون فیلد» در نظر می‌گیرد. سپس ۴ تست جدید نوشته شد و **تأیید شد که سه‌تای آن‌ها روی کد قدیمی قرمز می‌شوند** — یعنی باگ واقعاً پین شده است. رفع، منتشر و روی production دوباره تست شد ⇒ ۳۰۳.

**۲) فرار خطا از بلوک try/catch.** مسیرها promise را بدون `await` برمی‌گرداندند، پس رد شدن promise از `try` بیرون می‌زد و به جای صفحهٔ ۵۰۰ تمیز، خطای خام برمی‌گشت. با `await` صریح رفع شد (تست شبیه‌سازی قطعی D1 این را گرفت).

**۳) خروجی build در مسیر اشتباه.** `wrangler` مقدار `--outdir` را نسبت به فایل config حساب می‌کند، پس خروجی در `admin/admin/dist` می‌نشست و از قاعدهٔ ignore فرار می‌کرد؛ در نتیجه lint روی کد تولیدشده خطا می‌داد. مسیر و الگوی ignore اصلاح شد.

**۴) اولویت اشتباه در fixture تست.** الگوی کلی `FROM users` روی کوئری مخاطبان پیام همگانی سایه انداخته بود. الگوها دقیق شدند.

**۵) `WEBHOOK_SECRET` گم‌شده.** این مقدار در فایل credential موجود نبود، پس تست زندهٔ مسدودسازی ۴۰۱ می‌گرفت. Secret چرخانده شد، در تلگرام `setWebhook` دوباره تنظیم و مقدار جدید خارج از مخزن ذخیره شد. سپس تست end-to-end انجام شد.

---

## ۶. تست زندهٔ production

پس از انتشار، روی سرویس واقعی انجام شد:

- `/health` ⇒ ۲۰۰ · بازدید ناشناس ⇒ هدایت به `/login` · POST ناشناس ⇒ ۴۰۱ · مسیر ناشناخته ⇒ ۴۰۴
- رمز غلط ⇒ ۴۰۱ با پیام مبهم · رمز درست ⇒ مرحلهٔ کد + ارسال واقعی به تلگرام · کد غلط ⇒ ۴۰۱ · **کد سوخته دوباره استفاده شد ⇒ ۴۰۱**
- ورود کامل ⇒ کوکی با `HttpOnly; Secure; SameSite=Strict`
- هر ۷ صفحهٔ محافظت‌شده ⇒ ۲۰۰ و **هیچ نشت رمز یا توکنی** در HTML
- POST از مبدأ بیگانه ⇒ **۴۰۳** · از مبدأ خودی ⇒ ۳۰۳
- **زنجیرهٔ کامل مسدودسازی:** مسدود کردن از پنل ⇒ رکورد در D1 و کلید در KV ⇒ ارسال یک update واقعی به webhook ربات از طرف کاربر مسدود ⇒ **هیچ ردی در پایگاه داده ثبت نشد**، در حالی که کاربر کنترل (مسدودنشده) در همان لحظه ثبت شد.
- رفع مسدودی و حذف کامل ⇒ ۳۰۳ و صفر ردیف باقی‌مانده (**تمام داده‌های تستی پاک شد**)
- خروج ⇒ نشست سمت سرور باطل شد و کوکی قبلی دیگر پذیرفته نشد
- تمام اقدامات در جدول رویدادها ثبت شدند

---

## ۷. اطلاعات محرمانه

هیچ رمز، توکن یا کلیدی در مخزن، در این گزارش یا در commitها نوشته نشده است.

Secretهای Worker پنل (فقط از طریق `wrangler secret put` تنظیم شدند):

| نام | کاربرد |
|---|---|
| `ADMIN_PASSWORD` | عامل اول ورود — به صورت تصادفی تولید شد |
| `SESSION_SECRET` | امضای HMAC نشست‌ها — ۳۲ بایت تصادفی |
| `BOT_TOKEN` | ارسال کد ورود و پیام‌ها از طریق تلگرام |
| `CF_ANALYTICS_TOKEN` | خواندن مصرف سهمیه از GraphQL Analytics — حداقل دسترسی لازم: **Account → Account Analytics → Read** |

متغیر غیرمحرمانهٔ `CF_ACCOUNT_ID` در `admin/wrangler.jsonc` قرار دارد (شناسهٔ حساب، در آدرس هر صفحهٔ داشبورد Cloudflare دیده می‌شود و توکن نیست).

**رمز ورود پنل** در فایل `/tmp/creds/admin.sh` (خارج از مخزن، دسترسی ۶۰۰) ذخیره شده است. توجه: مسیر `/tmp` ماندگار نیست — لطفاً رمز را جای امنی (مدیر رمز عبور) کپی کنید. برای تغییر آن:

```
npx wrangler secret put ADMIN_PASSWORD --config admin/wrangler.jsonc
```

> **چرخش کلید:** در جریان تست زندهٔ این مرحله، مقدار `WEBHOOK_SECRET` عمداً با یک مقدار تصادفی جدید جایگزین شد (هم روی Worker و هم با `setWebhook` روی تلگرام) تا بتوان بدون دانستن مقدار قبلی، update واقعی به Webhook فرستاد. Webhook پس از آن سالم است: بدون خطا و بدون صف انباشته.

صفحهٔ «تنظیمات ربات» مقدار `WEBHOOK_SECRET` را فقط به عنوان ورودی موقت می‌گیرد و **هرگز ذخیره یا در جدول رویدادها ثبت نمی‌کند** (تست اختصاصی دارد).

---

## ۸. محدودیت‌های شناخته‌شده

- **دسترسی به پنل فقط با رمز است، نه با IP یا Cloudflare Access.** اگر خواستید سخت‌گیرانه‌تر شود، می‌توان Cloudflare Access را جلوی این Worker گذاشت.
- پیام همگانی در دسته‌های ۲۵ تایی و در پس‌زمینه ارسال می‌شود و سقف ۵۰۰۰ گیرنده دارد؛ برای جمعیت بزرگ‌تر باید صف (Queues) اضافه شود.
- نمودار داشبورد با CSS ساخته شده (بدون کتابخانهٔ نمودار) تا با CSP سخت‌گیرانه سازگار بماند.
